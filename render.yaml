services:
  - type: web
    name: nextraj-ai
    runtime: python

    buildCommand: |
      pip install -r requirements.txt

    startCommand: |
      gunicorn app:app

    envVars:
      - key: GEMINI_API_KEY
        sync: false

      - key: GROQ_API_KEY
        sync: false